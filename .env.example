# To ensure the interview simulator function follow the next steps:
# 1. Create .env file in the repository root named ".env".
# 2. Insert the API key in the file: "openAI_api_interview_key=The_key_given_to_you".
# 3. Ensure to run "flutter pub get" after creating the file and assigning the API key value. 
